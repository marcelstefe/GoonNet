<ui:FluentWindow x:Class="GoonNet.MassImportWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
                 xmlns:dgprim="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
                 Title="Mass Import Files"
                 Width="820"
                 Height="620"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="None"
                 WindowCornerPreference="Round"
                 FontFamily="pack://application:,,,/res/fonts/#Inter Variable">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </ui:FluentWindow.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Mass Import Files" Height="48"
                     ShowMinimize="False" ShowMaximize="False"/>

        <Border Grid.Row="1" Margin="12,8,12,8"
                Background="{DynamicResource AppCardBg}" CornerRadius="8">
            <Grid Margin="24,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:Button Grid.Column="0" Content="Add Files..." Height="36" Padding="14,0"
                               Click="AddFilesButton_Click"/>
                    <ui:Button Grid.Column="1" Content="Add Folder..." Height="36" Padding="14,0" Margin="8,0,0,0"
                               Click="AddFolderButton_Click"/>
                    <CheckBox Grid.Column="2" x:Name="IncludeSubfoldersBox" Content="Include subfolders"
                              IsChecked="True" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:Button Content="Clear" Height="36" Padding="14,0" Margin="0,0,16,0"
                                   Click="ClearButton_Click"/>
                        <TextBlock Text="Type" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="CategoryBox" Height="36" Width="150"
                                  Style="{StaticResource AppComboBoxStyle}" SelectedIndex="0">
                            <ComboBoxItem Content="Song"/>
                            <ComboBoxItem Content="Station ID"/>
                            <ComboBoxItem Content="Promo"/>
                            <ComboBoxItem Content="Commercial"/>
                            <ComboBoxItem Content="News"/>
                            <ComboBoxItem Content="Bed"/>
                            <ComboBoxItem Content="Voice Track"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Files and folders can also be dropped straight onto the list -->
                <Border Grid.Row="1" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="8"
                        Background="#0f0f0f" ClipToBounds="True"
                        AllowDrop="True" DragOver="FileList_DragOver" Drop="FileList_Drop">
                    <Grid>
                        <DataGrid x:Name="FileGrid" Background="Transparent" BorderThickness="0"
                                  RowBackground="Transparent" AlternatingRowBackground="Transparent"
                                  Foreground="White"
                                  GridLinesVisibility="None"
                                  RowHeight="24"
                                  HeadersVisibility="Column"
                                  CanUserResizeRows="False"
                                  CanUserSortColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  SelectionMode="Extended"
                                  AutoGenerateColumns="False"
                                  PreviewKeyDown="FileGrid_PreviewKeyDown"
                                  Margin="8">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="dgprim:DataGridColumnHeader">
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#22FFFFFF"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1A1A1A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}"
                                                        SnapsToDevicePixels="True">
                                                    <ContentPresenter VerticalAlignment="Center"
                                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#454545"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <!-- Blank until the file's length has been read -->
                                <DataGridTextColumn Header="Length" Binding="{Binding Length, TargetNullValue='--:--'}" Width="70"/>
                                <DataGridTemplateColumn Header="Title" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Artist}" Opacity="0.55"
                                                           Margin="8,0,0,0" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="File" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FilePath}" ToolTip="{Binding FilePath}"
                                                       Opacity="0.55" TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="" Width="36">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Width="20" Height="20" ToolTip="Remove"
                                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                                    Background="Transparent" BorderThickness="0" Padding="0"
                                                    Cursor="Hand" Click="RemoveButton_Click"
                                                    Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource BoolToVis}}">
                                                <svgc:SvgViewbox Source="/res/studio/trash.svg" Width="12" Height="12"/>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="EmptyText" Text="Add files or a folder, or drop them here."
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="12" Foreground="#808080" IsHitTestVisible="False"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="12,4,14,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Margin="4,0,12,0" FontSize="12"
                       Foreground="#808080" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border x:Name="ImportButton" BorderThickness="0"
                        CornerRadius="12" Padding="15,4" Margin="0,0,8,0" Cursor="Hand"
                        Style="{StaticResource AppPrimaryPillStyle}"
                        MouseLeftButtonUp="ImportButton_Click">
                    <TextBlock Text="Import" Foreground="White" FontSize="12" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Border>
                <Border BorderThickness="1.4" BorderBrush="Gray"
                        CornerRadius="12" Padding="15,4" Cursor="Hand"
                        Style="{StaticResource AppSecondaryPillStyle}"
                        MouseLeftButtonUp="CancelButton_Click">
                    <TextBlock Text="Cancel" Foreground="Gray" FontSize="12" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
